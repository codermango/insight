/*
 *
 * UserInsightDashboard
 *
 */

import React from 'react';
import styles from './styles.css';

export default class UserInsightDashboard extends React.Component { // eslint-disable-line react/prefer-stateless-function
  render() {
    return (
      <div className={styles.userInsightDashboard}>
        User Insight Dashboard
      </div>
    );
  }
}
